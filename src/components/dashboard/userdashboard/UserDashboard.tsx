import React from 'react';
import Cards from './Cards';

const UserDashboard = () => {
    return (
        <div>
            <Cards/>
            
        </div>
    );
};

export default UserDashboard;